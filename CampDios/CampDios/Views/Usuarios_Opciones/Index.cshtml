@model IEnumerable<CampDios.Modelos.Usuarios_Opciones>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Lectura)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Escritura)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Edicion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Opciones.Nombre_Opciones)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuarios.Login)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Lectura)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Escritura)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Edicion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Opciones.Nombre_Opciones)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuarios.Login)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idUsuario_Opciones }) |
            @Html.ActionLink("Details", "Details", new { id=item.idUsuario_Opciones }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idUsuario_Opciones })
        </td>
    </tr>
}

</table>
