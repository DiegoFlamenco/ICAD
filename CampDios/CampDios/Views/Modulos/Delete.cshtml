@model CampDios.Modelos.Modulos

@{
    ViewBag.Title = "Borrar Modulo";
}

<h2>Eliminar</h2>

<h3>¿Esta seguro que desea eliminar este Módulo?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Nombre_Modulos)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Nombre_Modulos)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Borrar" class="btn btn-danger" /> &nbsp;&nbsp;
            <a href="@Url.Action("Index","Modulos")" class="btn btn-default">
                <span class="glyphicon glyphicon-arrow-left" aria-hidden="true"></span> Volver al listado
            </a>
        </div>
    }
</div>
